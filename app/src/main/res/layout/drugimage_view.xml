<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/drugimage_text"
        android:layout_width="380dp"
        android:layout_height="380dp"
        android:layout_margin="4dp"
        android:background="@color/light_grey"
        android:gravity="center"
        android:adjustViewBounds="true"
        android:scaleType="centerInside"
        android:src="@drawable/camera" 
        android:contentDescription="@string/txt_imagecontentdesc">
    </ImageView>

    <!--
        <ImageView
	    android:id="@+id/cross_img"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_gravity="right|bottom"
	    android:visibility="gone"
	    android:background="@drawable/overflow" />
    -->

    <ImageView
        android:id="@+id/cross_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/drugimage_text"
        android:layout_alignRight="@id/drugimage_text"
        android:layout_gravity="right|bottom"
        android:paddingLeft="8dp"
        android:paddingTop="8dp"
        android:src="@drawable/dropdown"
        android:visibility="gone" 
        android:contentDescription="@string/txt_imagecontentdesc"/>

</RelativeLayout>